import {TableTabs} from '@site/src/components/table/table.jsx';
import {componentQreable} from './schemes/schemeQqreable.jsx'

# Qreable
Show modal about QR content.

<TableTabs tabsContent={componentQreable} />

## Usage

1. Import the `qreable` component and define it within your component:

  ```js
  import qreable from "@imagina/qqreable/_components/qreable.vue"
  ```

2. Use the `qreable` component:

 ```jsx
 <qreable ref="qreableComponent" />
 ```